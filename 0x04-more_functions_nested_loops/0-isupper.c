#include "main.h"
/**
* _isupper - prints the alphabet in lowercase, followed by a new line
* @c : super story
* return : 1 0r 0
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
