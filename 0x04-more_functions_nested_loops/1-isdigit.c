#include "main.h"
/**
* _isdigit - prints the alphabet in lowercase, followed by a new line.
* @c: super story
* Return: 1 0r 0
*/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
